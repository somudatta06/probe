"""probe — deterministic, local-first incident-evidence engine for AI agents.

Reference implementation (Python). The hot path (templating + scoring) ports to
Rust/Go for the shipped single binary. See README.md.
"""
__version__ = "0.0.1"
